{
  "name": "hyperapp-demo",
  "version": "1.0.0",
  "description": "demo of hyperapp",
  "scripts": {
    "clean": "rm -rf dist .cache",
    "prebuild": "npm run clean",
    "build": "parcel build --public-url '.' --no-source-maps --detailed-report src/index.html",
    "start": "parcel serve src/index.html"
  },
  "dependencies": {
    "hyperapp": "^1.2.9"
  },
  "devDependencies": {
    "parcel": "^1.10.3",
    "sass": "^1.14.3",
    "typescript": "^3.1.6"
  }
}
